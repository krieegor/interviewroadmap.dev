node_modules
.next
out
coverage
public
.agents
.claude
remotion
.wrangler

# Conteúdo MDX é formatado manualmente pelo autor. O plugin MDX do Prettier reflui texto JSX para o
# printWidth, o que corrompe tabelas GFM (`| a | b |`) dentro de componentes como <Comparacao>.
src/content/**/*.mdx
