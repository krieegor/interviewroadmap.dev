---
id: 36
title: "Does Kafka have native DLQ?"
slug: "o-kafka-possui-dlq-nativa"
level: ["senior", "tech-lead"]
topics: ["consumo"]
relatedChapters: ["retry-e-dlq"]
shortAnswer: "No. Unlike SQS, Kafka has no native dead-lettering feature — a DLQ is just an ordinary topic, populated by application convention or by a framework like Spring Kafka."
quiz:
  options:
    - "No. Unlike SQS, Kafka has no native dead-lettering feature — the DLQ is an ordinary topic, populated by application convention or by a framework."
    - "Yes, any Kafka topic can have a DLQ enabled through configuration, equivalent to SQS's RedrivePolicy."
    - "Yes, but only when the cluster is managed by a cloud provider, which adds this feature as a broker extension."
    - "No, and because of that Kafka offers no practical way to isolate problematic messages from the rest of the flow."
  correctIndex: 0
---

## Question

"Does Kafka have a native DLQ mechanism, like SQS does?"

## What the interviewer wants to assess

This is a frequent, revealing trap question — it tests whether the candidate assumes, by analogy with SQS,
that Kafka has DLQ "out of the box."

<RespostaCurta>
No. Unlike SQS, which has DLQ natively built into the queue, Kafka has no native dead-lettering feature at
all. A "DLQ" in Kafka is, in practice, just an ordinary topic — the logic to publish problematic messages
to it is the application's or a framework's responsibility.
</RespostaCurta>

<RespostaSenior>
SQS has a native configuration attribute (`RedrivePolicy`) that AWS manages internally. Kafka has no
equivalent — the "DLQ topic" is an architectural convention, not a platform feature: you create a normal
topic (for example, `<topic>.DLT`) and implement the logic to publish to it once retry attempts are
exhausted. Spring Kafka makes this easier with `DeadLetterPublishingRecoverer`, which integrates with
`DefaultErrorHandler` and automatically publishes to the configured DLQ topic — but that's a framework
abstraction on top of Kafka, not a native broker capability.
</RespostaSenior>

## In-depth explanation

See "DLQ in Kafka: it's not native" in [Chapter 9](/en/livro/retry-e-dlq) and the practical implementation
in [Chapter 14](/en/livro/consumer-com-spring-kafka).

<ExemploFinanceiro>
A team migrating from SQS to Kafka needs to explicitly implement the DLQ logic (via
`DeadLetterPublishingRecoverer` or equivalent) — there's no Kafka topic configuration parameter that
enables this automatically, unlike SQS.
</ExemploFinanceiro>

<Pegadinha title='"Sure, any Kafka topic can have a DLQ configured on itself"'>
There's no topic configuration that enables DLQ automatically. The DLQ is always a separate topic, created
and populated by explicit application or framework logic — never an attribute of the original topic.
</Pegadinha>

<PerguntaDerivada>
Likely follow-ups: "how would you implement a DLQ in Kafka?" and "what is a Poison Pill?".
</PerguntaDerivada>
