---
id: 11
title: "What is a Broker?"
slug: "o-que-e-um-broker"
level: ["pleno", "senior"]
topics: ["arquitetura"]
relatedChapters: ["arquitetura-interna", "brokers-lideres-e-replicacao"]
shortAnswer: "A Broker is a running Kafka process that stores a subset of the cluster's partitions and serves read and write requests for them; a cluster is made up of multiple brokers."
quiz:
  options:
    - "A Broker is a running Kafka process that stores a subset of the cluster's partitions and serves read and write requests for them."
    - "A Broker is just the name for the physical or virtual machine that hosts the Kafka process."
    - "A Broker is the process that only coordinates cluster metadata, without storing any topic partitions."
    - "Every broker in the cluster keeps a full copy of every partition for every topic."
  correctIndex: 0
---

## Question

"What is a Broker in Kafka?"

## What the interviewer wants to assess

Whether you understand the broker as the real infrastructure unit behind the topic/partition abstraction —
not just as a vague synonym for "Kafka server."

<RespostaCurta>
A Broker is a running Kafka process. It physically stores the data for one or more partitions and responds
to producer (write) and consumer (read) requests for those partitions. A Kafka cluster is made up of
multiple brokers working together.
</RespostaCurta>

<RespostaSenior>
A broker is Kafka's storage and serving unit: a process that keeps on disk the log files for a subset of
the partitions across every topic in the cluster. For each partition it hosts, a broker acts as leader
(serving all of its reads and writes) or as follower (passively replicating the leader's data, Chapter 5).
A cluster spreads partitions across its brokers to parallelize load and tolerate losing any one of them,
as long as the configured replication factor allows it.
</RespostaSenior>

## In-depth explanation

See "Broker" in [Chapter 3](/en/livro/arquitetura-interna) and the deep dive on replicas in
[Chapter 5](/en/livro/brokers-lideres-e-replicacao).

<ExemploFinanceiro>
A cluster of 3 brokers hosting the `pagamentos.aprovados` topic (12 partitions, replication factor 3)
spreads those partitions and their replicas across the 3 brokers in a balanced way — no single broker
concentrates all of the topic's write traffic.
</ExemploFinanceiro>

<Pegadinha title='"Broker is just a fancy name for server"'>
Broker specifically refers to the Kafka process, not the physical or virtual machine hosting it. It's
common (but not required) to run one broker per machine in production, but the distinction between "broker
process" and "machine" matters when discussing scale and replica distribution.
</Pegadinha>

<PerguntaDerivada>
Likely follow-ups: "what is a Kafka Cluster?" and "what happens when a broker goes down?".
</PerguntaDerivada>
