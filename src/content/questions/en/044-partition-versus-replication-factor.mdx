---
id: 44
title: "What's the difference between Partition and Replication Factor?"
slug: "partition-versus-replication-factor"
level: ["pleno", "senior"]
topics: ["arquitetura", "comparacoes"]
relatedChapters: ["partitions-e-ordenacao", "brokers-lideres-e-replicacao"]
shortAnswer: "Partition defines how many independent slices a topic is split into (parallelism and scale). Replication Factor defines how many copies exist of each of those slices (availability)."
quiz:
  options:
    - "Partition defines how many independent slices a topic has (parallelism and scale); Replication Factor defines how many copies exist of each slice (availability) — independent axes."
    - "Increasing Replication Factor increases consumption parallelism, because additional replicas can be read simultaneously by different consumers."
    - "Partition and Replication Factor are the same configuration, just different names for a topic's number of copies."
    - "Increasing a topic's number of partitions directly increases its tolerance to broker loss."
  correctIndex: 0
---

## Question

"What's the difference between a topic's number of Partitions and its Replication Factor?"

## What the interviewer wants to assess

A common confusion among beginners is treating the two as the same "scale" thing. This question separates
candidates who understand a topic's two independent design axes.

<RespostaCurta>
Partition defines how many independent slices a topic is split into — the basis for consumption
parallelism and write load distribution. Replication Factor defines how many copies exist of each of those
partitions, on different brokers — the basis for fault tolerance.
</RespostaCurta>

<RespostaSenior>
They're two completely independent design axes. A topic with 12 partitions and replication factor 3 has,
in practice, 36 physical replicas spread across the cluster (12 partitions × 3 copies each). Increasing
partitions increases consumption parallelism and write scale, but doesn't affect availability by itself.
Increasing replication factor increases tolerance to broker loss, but doesn't affect parallelism — more
replicas of the same partition don't let more consumers process it simultaneously, because client reads
and writes only happen through that partition's leader.
</RespostaSenior>

## In-depth explanation

See [Chapter 4](/en/livro/partitions-e-ordenacao) (partitions) and
[Chapter 5](/en/livro/brokers-lideres-e-replicacao) (replication factor).

<ExemploFinanceiro>
A `pix.recebido` topic with 24 partitions and replication factor 3 has 24 leaders and 48 follower replicas
spread across the cluster's brokers — 24 is the consumption parallelism ceiling; 3 is the fault tolerance
of each of those 24 slices.
</ExemploFinanceiro>

<Pegadinha title='"More Replication Factor means more consumption parallelism"'>
No — additional replicas of a partition exist exclusively for fault tolerance (leader failover). They
aren't read by consumers in parallel with the leader; only the leader serves clients in the default
configuration.
</Pegadinha>

<PerguntaDerivada>
Likely follow-ups: "what are Leader and Follower?" and "what happens when the Leader goes down?".
</PerguntaDerivada>
