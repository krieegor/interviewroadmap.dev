---
id: 17
title: "How does Kafka choose a Partition?"
slug: "como-o-kafka-escolhe-uma-partition"
level: ["pleno", "senior"]
topics: ["arquitetura"]
relatedChapters: ["partitions-e-ordenacao"]
shortAnswer: "If the message has a key, the partitioner computes hash(key) % number of partitions, always resulting in the same partition for the same key; without a key, distribution is approximately uniform (sticky strategy)."
quiz:
  options:
    - "With a key, the partitioner deterministically computes hash(key) % number of partitions; without a key, distribution is approximately uniform."
    - "Without a key set, Kafka picks a completely random partition for every message sent, with no batching pattern."
    - "The destination partition is decided by the broker that receives the message, not by the producer's partitioner."
    - "Even with a key set, messages with the same key can land on different partitions depending on cluster load."
  correctIndex: 0
---

## Question

"How does Kafka decide which Partition a message will be stored in?"

## What the interviewer wants to assess

Whether you understand the partitioning mechanism as deterministic and tied to the key — the basis for the
almost inevitable next question about ordering.

<RespostaCurta>
If the message has a key, the default partitioner computes `hash(key) % number_of_partitions` — since the
hash of the same key is always the same, it consistently lands on the same partition. Without a key, Kafka
distributes messages across partitions in an approximately uniform way.
</RespostaCurta>

<RespostaSenior>
The producer's partitioner decides this at send time. With a key set, the computation is deterministic:
`hash(key) % number_of_partitions`, guaranteeing every message with that key always goes to the same
partition — which preserves relative ordering among messages with the same key. Without a key, recent
versions of the Java client use a *sticky* strategy, grouping batches of messages into one partition at a
time before rotating, optimizing network batching without compromising overall distribution across
partitions. You can also provide a custom `Partitioner` for cases where the default distribution logic
doesn't fit (for example, prioritizing a specific partition for VIP customers).
</RespostaSenior>

## In-depth explanation

See "Message key and the partitioning decision" in [Chapter 4](/en/kafka/livro/partitions-e-ordenacao).

<ExemploFinanceiro>
When publishing events to the `pix.recebido` topic using `contaId` as the key, every event for the same
account is deterministically routed to the same partition — letting the `saldo-service` process that
account's events strictly in the order they occurred.
</ExemploFinanceiro>

<Pegadinha title='"The partition is chosen randomly on every send"'>
Even without a key, distribution isn't "purely" random per message — modern client versions use a sticky
strategy that groups messages by batch before rotating partitions, optimizing network throughput. And with
a key, the choice is always deterministic, never random.
</Pegadinha>

<PerguntaDerivada>
Likely follow-ups: "what is a Message Key?" and "how does ordering work in Kafka?".
</PerguntaDerivada>
